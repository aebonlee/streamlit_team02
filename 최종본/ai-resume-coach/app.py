# 새 폴더 생성
mkdir ai-resume-coach
cd ai-resume-coach

# app.py 파일 생성 (첨부하신 코드 전체 복사)
# requirements.txt 파일 생성 (위 내용)
# render.yaml 파일 생성 (위 내용)

# Git 초기화 및 푸시
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/ai-resume-coach.git
git push -u origin main
